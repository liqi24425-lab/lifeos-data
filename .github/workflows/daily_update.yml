name: Daily Content Update

on:
  schedule:
    # 每天北京时间早上 8:00 (UTC 0:00)
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run Publish Script
        run: python publish.py

      - name: Commit and Push changes
        run: |
          git config --global user.name 'LifeOS Bot'
          git config --global user.email 'bot@lifeos.app'
          # 👇 重点在这里：我加上了 feed.json
          git add feed.json queue.json
          # 为了防止第一次没有 feed.json 报错，加个保护逻辑
          git diff-index --quiet HEAD || git commit -m "Auto-publish daily content"
          git push
